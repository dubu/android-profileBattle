<?xml version="1.0" encoding="utf-8"?>
<!-- main.xml 카메라 샘플 레이아웃 -->
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" android:orientation="vertical">
    <!-- 미리보기 -->
    <SurfaceView
            android:id="@+id/surface_view"
            android:layout_gravity="center_horizontal"
            android:layout_height="160dp"
            android:layout_width="200dp"/>
    <!-- 셔터 버튼 -->
    <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/shutter"
            android:src="@android:drawable/ic_menu_camera" android:layout_gravity="center_horizontal"/>
    <ImageView
            android:layout_height="160dp"
            android:layout_width="200dp"
            android:id="@+id/image_view" android:layout_gravity="center_horizontal"/>
    <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/img_list" android:layout_gravity="center"
            android:src="@android:drawable/ic_menu_sort_by_size"/>
    <!-- 가져온 이미지를 표시 -->
</LinearLayout>